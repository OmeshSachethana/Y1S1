#include<stdio.h>

int main()
{
	int num, max, min;
	printf("Enter numbers: ");
	scanf("%d", &num);
	
	max = 0;
	min = num;
	while(num != -99){
		while(num > 100 || num < 0){
			printf("Invaild Number. Reenter marks");
			scanf("%d", &num);
		
			if(num > max){
				max = num;
			}
			else if(num < min)
				min = num;
}
			printf("Enter numbers: ");
			scanf("%d", &num);
	}
		
		printf("Maximum No: %d", max);
		printf("Minumum No: %d", min);	
	return 0;
}
